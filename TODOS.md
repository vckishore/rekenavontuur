# TODOS — Rekenavontuur

Auto-generated by /autoplan on 2026-03-29. Branch: master.

---

## P1 — Shipping Blockers (before sharing with anyone)

- [ ] **Translate Codex prompt to Flemish Dutch** (`app/codex_pipeline.py:115-121`)
  Runtime-generated problems come out English. Fix the prompt to instruct Codex to generate
  Flemish Belgian Dutch. Add `story`, `question`, `hint` language instruction.

- [ ] **Fix English error string in backend** (`app/main.py:85`)
  `"Please enter a whole number"` shows to Dutch-speaking kids. Change to
  `"Voer een heel getal in"`.

- [ ] **Write README.md**
  Success criterion: another parent can self-host following only the README.
  Must cover: Python 3.11+, Node 18+, `pip install -r requirements.txt`,
  `cd frontend && npm install`, how to run (two terminals), Windows-specific
  notes, `MATH_DB_PATH` env var, Codex pipeline docs.

- [ ] **Add `lang="nl"` to `<html>` element** (`frontend/index.html`)
  Screen readers default to system language without this.

## P2 — Quality (before open-sourcing)

- [ ] **Fix Skip button on last problem** (`frontend/src/components/PracticeScreen.jsx:100`)
  `skipProblem()` silently does nothing when `current === problems.length - 1`.
  Either hide the Skip button on the last problem, or skip → session complete.

- [ ] **Add missing pytest coverage** (`tests/test_api.py`, `tests/test_pipeline.py`)
  Test plan items not yet covered:
  - `POST /api/answers` with "2.5" → parse error (item #5)
  - `POST /api/answers` records `attempt_number=2` on retry (item #6)
  - `GET /api/dashboard` with 2 sessions → per-topic breakdown (item #8)
  - `GET /api/export/csv` with rows — check all columns present (item #10)
  - `GET /api/admin/status` — pool health (not in test plan but untested)
  - `generate_problems()` when codex not on PATH — graceful (item #11)
  - Pipeline retries once when <5 valid (item #16)
  - Pipeline gives up after 2 failures, writes to error log (item #17)
  - Duplicate problem not written (item #18)
  - UUID IDs unique across runs (item #19)

- [ ] **Pin exact versions in `requirements.txt`**
  Before open-sourcing: run `pip freeze` and pin all transitive deps.

- [ ] **Add `aria-label` to ProgressDots**
  Screen readers say nothing useful for `<span data-testid="dot-filled">`.
  Add `aria-label="Vraag 2 van 5 voltooid"` or similar.

## P3 — Nice to Have (v1.1+)

- [ ] **Topic/grade selector on PracticeScreen**
  `TOPIC` and `GRADE` are hardcoded (`PracticeScreen.jsx:6-7`).
  Add a simple topic/grade picker so a second kid at a different grade can use the app.

- [ ] **Session complete screen — add celebration**
  "Oefensessie voltooid!" is text-only. A CSS color flash, confetti, or
  cheerful message would go a long way for a 7-9 year old.

- [ ] **Deduplicate `load_problems()` calls in `start_session()`** (`app/main.py:42-51`)
  `load_problems()` called twice (lines 42 and 51 via `pool_count()`).
  Store result and reuse.

- [ ] **E2E tests with Playwright**
  Two flows marked `[→E2E]` in test plan:
  1. Complete session: load app → Start → answer 5 → session complete
  2. Parent dashboard: complete session → /dashboard → stats reflect within 1s

- [ ] **CI/CD with GitHub Actions** (add at open-source milestone)
  pytest + vitest on push + PR. Add when repo is made public.

- [ ] **PDF export** (v1.1 — school format TBD)

- [ ] **Multi-kid profiles** (v2 — users table + profile picker)

- [ ] **Admin UI for problem editing** (v2)
